{% extends 'base.html.twig' %}

{% block title %}Modifier le livre{% endblock %}

{% block main %}
<div class="container mt-5">
    <h1 class="mb-4">Modifier le livre</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary mt-3 rounded-pill">Enregistrer</button>
    {{ form_end(form) }}
</div>
{% endblock %}
